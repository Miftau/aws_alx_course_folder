from setuptools import setup

setup(name= 'distributions',
      version= '1.0',
      description= 'Normal Distribution',
      packages= ['distribution'],
      author= 'PROGRESS ICT MEDIA CONCEPTS',
      zip_safe = False)